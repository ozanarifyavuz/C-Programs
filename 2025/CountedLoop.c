#include <stdio.h>

int main(int argc, char const *argv[]) {
    int i;
    for (i = 0; i <= 5; i++) {
        /* You can write your codes in the following lines - This is to learn how to comment in C. */
        printf("%d\n", i);
    }
    return 0;
}